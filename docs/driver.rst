The Driver: main.py
===================

``main.py`` is the repo-root driver + CLI: ``compile()`` parses, solves and
verifies one circuit; ``main()`` prints one verified CSV row per circuit and,
for a single circuit, renders the glTF scene (``-o``) or saves the tile
program (``--json``).

.. automodule:: main
   :members:
